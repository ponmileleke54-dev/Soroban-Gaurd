[package]
name = "soroban-guard-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-guard-core = { path = "../soroban-guard-core" }
clap = { version = "4.4", features = ["derive"] }
colored = "2.1"
serde_json = "1.0"